# App Info
spring.application.name=ShareVault

# Server Port
server.port=8989

# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/share_vault
spring.datasource.username=root
spring.datasource.password=Root123$

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# File Upload Directory (Use double backslashes for Windows)
file.upload-dir=C:\\ShareVault\\ShareVault\\ShareVault\\DocSharingServer\\uploads

